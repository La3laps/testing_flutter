name: testing_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  english_words: ^4.0.0
  provider: ^6.0.0
  http: ^1.5.0
  flutter_json_viewer: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  test: ^1.26.2

flutter:
  uses-material-design: true
